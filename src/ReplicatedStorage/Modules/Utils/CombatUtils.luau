--purpose: provide lists of functions that are often used for combat


return {

    ApplyForce = function(BasePart : BasePart)
      
    end,

    ApplyKnockback = function(Force : number, IgnoreList : {Player})

    end,

    Explosion = function(Epicenter : Vector3 , Force : number, IgnoreList : {Player})

    end,
}